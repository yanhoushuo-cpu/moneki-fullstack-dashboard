# 默认 mock 模式无需任何外部服务，数值仍实时查询 SQLite。
AI_MODE=mock

# 如需启用 OpenAI-compatible 模型规划器，改为 provider 并填写以下变量。
# AI_MODE=provider
# AI_API_KEY=
# AI_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-5.4-mini
