# 默认 mock 模式无需任何外部服务，数值仍实时查询 SQLite。
AI_MODE=mock

# Render 等托管平台会自动注入 PORT；本地不设置时使用 8000。
# PORT=8000

# 如需自行启用 OpenAI-compatible 模型规划器，改为 provider 并填写以下变量。
# 公开演示默认不启用 provider，也不会提交任何真实密钥。
# AI_MODE=provider
# AI_API_KEY=
# AI_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-5.4-mini
