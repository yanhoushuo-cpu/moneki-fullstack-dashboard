# 店务罗盘演示脚本

建议演示时长 2–3 分钟。先打开 `http://localhost:8000`，保持默认完整日期范围（2026-05-01 至 2026-07-31）。

## 0. 先证明看板不是空壳

顶部 KPI 应显示：

- 净营业额：**¥429,637.00**
- 唯一订单：**11,941**
- 客单价：**¥35.98**
- 有效记录：**11,944**

切换趋势指标，选择一个门店，再打开右上角“数据质量”抽屉。指出 76 行重复、119 行安全补全、111 行唯一隔离记录均有规则说明。

## 1. 必问题：哪个品类的门店营业额最高？

在 AI 助手点击或输入：

> 哪个品类的门店营业额最高？

预期回答：

> 营业额最高的是“日料”，营业额为 **¥89,935.00**。

展开“1 条可核验证据”：

- tool：`get_top_entities`
- dimension：`store_category`
- date：2026-05-01 至 2026-07-31
- value_cents：`8,993,500`
- order_count：`2,455`

这证明结果通过 `sales.store_id → stores.store_id` JOIN 获取品类，而不是从销售表猜门店信息。

## 2. 必问题：牛肉poke 六月卖了多少钱？

> 牛肉poke 六月卖了多少钱？

发送后先观察回答逐段出现，并确认完成前显示“流式传输 · 数据库规则分析”。这证明页面正在消费 `/api/v1/chat/stream`，同时如实说明内容由本地规则规划器组织，而不是在线大模型。

预期回答：

> 牛肉poke 在 2026-06-01 至 2026-06-30 的营业额为 **¥13,818.00**，覆盖 **185** 个订单。

Evidence 中应看到：

- tool：`get_revenue`
- product_name：`牛肉poke`
- revenue_cents：`1,381,800`
- average_order_value_cents：`7,469`

点击“应用到看板”，日期应自动切换为 6 月，Top 商品表中的牛肉poke 被高亮。看板与 AI 使用同一个查询服务。

## 3. 必问题：客单价最近是涨了还是跌了？

> 客单价最近是涨了还是跌了？

预期回答：

> 最近一个完整月的客单价为 **¥36.04**，相比上月**上涨 2.3%**。

Evidence 展示：

- tool：`compare_periods`
- 2026-07-01 至 2026-07-31：`3,604` 分
- 2026-06-01 至 2026-06-30：`3,522` 分
- change_percent：`2.3`
- direction：`up`

## 4. 上下文追问

紧接第 2 题之后问：

> 那五月呢？

预期回答会继承“牛肉poke”和营业额，只把月份改成五月：**¥13,146.00**，覆盖 **191** 个订单。Evidence 参数应为 2026-05-01 至 2026-05-31，客单价为 `6,883` 分。

## 5. 无法回答的问题

> 明天北京会下雨吗？

预期：状态为 `unsupported`，回答“这个问题超出了当前销售数据可以回答的范围”，evidence 为空，并给出可回答的问题建议。系统不会编造天气。

## 数字为什么可信

1. 看板与 AI 都调用 `AnalyticsService`。
2. AI 只能调用五个白名单工具，不能读取 CSV 或执行自由 SQL。
3. 每个回答都带规范化参数、原始整数分结果和数据批次。
4. `backend/tests/ai/test_chat_contract.py` 将三个必问题和追问的 evidence 与数据库查询逐项比较。
5. `frontend/e2e/dashboard.spec.ts` 在真实 Chromium 中验证回答、证据展开和看板联动。

现场可运行：

```bash
python -m pytest backend/tests/ai/test_chat_contract.py -q
cd frontend && npm run test:e2e
```
