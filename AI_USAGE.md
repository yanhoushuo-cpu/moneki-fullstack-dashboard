# AI 使用说明

这份项目使用 OpenAI Codex 作为结对开发代理，协助完成数据剖析、架构设计、测试用例、代码实现、调试、文档和视觉验证。AI 没有被当成数字来源：所有经营数字都必须由测试覆盖的 SQLite 查询产生。

## 任务如何拆分

1. 先读取题目与三张 CSV，只做数据剖析，不改原始数据。
2. 写明数据规则、指标口径、AI 信任边界和验收标准。
3. 按 TDD 顺序实现：ETL → 分析服务 → REST → AI 工具 → React → 端到端。
4. 每个阶段单独提交，让 Git 历史可以还原设计演进。
5. 最后从真实数据库提取演示答案，并用浏览器在桌面、移动端复核。

## 一个真实 prompt

> 用一份约 10000 行的门店销售数据，做一个简单的数据看板（前端+后端）。技术栈完全自由。按推荐方案执行，不需要后续确认，你可以直接生成出来。要求尽快完成并尽量接近满分。

这个 prompt 只给出了目标和授权，并没有替我决定关键数据口径。AI 先形成书面设计规格和实施计划，再将每个可观察行为写成测试。

## AI 确实犯过的错误

### 错误 1：把规范化问题误报成脏外键

第一次数据剖析只对 `store_id` 做了 `trim`，没有统一大小写。AI 因此把 9 行 `s01` 当成未知门店，加上真实的 7 行 `S99`，初步声称有 **16 行未知门店**。这个数字是错的。

发现方式：ETL 集成测试将规范化销售行与门店维表 JOIN，并对真实数据做快照；测试显示小写 `s01` 应能关联 `S01`，否则有效行数和门店汇总同时不守恒。

修复方式：ID 规范化明确为 `strip + uppercase`，先增加 `s01 → S01` 测试，再修改 normalizer。最终可信数字为 **7 行 unknown_store**；规则写入数据质量文档和导入批次。

### 错误 2：质量百分比用了错误分母

AI 生成的第一版前端用 `valid / raw`，测试样例中把已去除的完全重复副本也算成数据缺陷，显示 75%。组件测试要求安全去重后没有隔离行时应显示 100%，因此立即暴露口径错误。

修复方式：改为 `valid / (raw - duplicates)`，同时在抽屉中分别展示原始、去重、修复、隔离和有效行数。真实数据因此显示约 99%，而不是为了好看硬写 100%。

## 如何阻止数字幻觉

- Planner 的返回对象只能是白名单工具调用或 `unsupported_reason`。
- Mock planner 源码中没有任何业务结果数字。
- Provider planner 使用严格 function schema，工具参数还会在服务器端二次校验。
- `ChatService` 只根据 `Evidence.result` 组织答案；evidence 包含参数、结果、批次和时间。
- AI 合同测试直接比较 `evidence` 与 `AnalyticsService` / SQL 结果，不解析自然语言凑答案。
- 前端把 evidence 展开给用户，并允许将同一参数应用到看板复核。

## 由开发者负责的决定

以下决定不能交给模型临场猜测，因此在代码和文档中固定：

- 营业额使用整数分累加；客单价是营业额除以唯一订单数。
- 日期区间两端包含，上一周期取相同天数且紧邻当前区间。
- 完全相同的七字段销售行才去重，保留首次出现。
- 缺失金额只在商品、正数量和有效单价均存在时补全。
- 未知外键、数量/金额符号冲突和不可信金额不自动修正，进入隔离表。
- 多个原因可以指向同一隔离行，汇总不能把原因数误当唯一坏行数。
- AI 不拥有 SQL 权限；Mock 和真实 provider 共用同一工具执行器。

## 哪些内容没有交给 AI 自由生成

- 数据清洗规则的业务含义和风险边界
- 指标定义与金额舍入方式
- 是否允许不确定记录进入经营报表
- 对不支持问题的产品行为
- 最终演示数字的确认：全部从数据库和自动化测试重新读取

AI 加快了实现，但最终可信度来自显式规则、守恒检查、可审计数据和自动化验证，而不是模型自信程度。
